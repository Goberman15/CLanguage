#include <stdio.h> // include std I/O lib to the code

int main()
{
  // before initialization, num holds indeterminate value
  int num;
  printf("Hello!\n");

  num = 10;
  printf("%d", num);

  return 0;
}
